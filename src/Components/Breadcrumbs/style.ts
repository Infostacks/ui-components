const style = {
  box: {
    // paddingLeft: "16px",
    color: "black",
    overflow: "hidden",
    whiteSpace: "nowrap",
    textOverflow: "ellipsis",
  },
  link: (theme) => ({
    color: theme.palette.text.primary,
    textDecoration: "none",
    fontSize: "16px",
    fontWeight: 400,
    lineHeight: "24px",
  }),
};

export default style;
